model AdminAreaSido {
  adm_cd   Int    @id
  adm_nm   String @db.Text
  x        Float
  y        Float
  polygons Json   @db.JsonB
  @@map("admin_area_sido")
}

model AdminAreaGu {
  adm_cd   Int    @id
  adm_nm   String @db.Text
  x        Float
  y        Float
  polygons Json   @db.JsonB
  @@map("admin_area_gu")
}

model AdminAreaDong {
  adm_cd   Int    @id
  adm_nm   String @db.Text
  x        Float
  y        Float
  polygons Json   @db.JsonB
  @@map("admin_area_dong")
}
