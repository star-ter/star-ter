model PopulationGrid {
    YMD      String  @db.VarChar(8)
    TT       String  @db.VarChar(2)
    CELL_ID  String  @db.VarChar
    H_DNG_CD String? @db.VarChar
    SPOP     Float?  @db.DoublePrecision

    // Male fields (00-70)
    M00 Float? @db.DoublePrecision
    M10 Float? @db.DoublePrecision
    M15 Float? @db.DoublePrecision
    M20 Float? @db.DoublePrecision
    M25 Float? @db.DoublePrecision
    M30 Float? @db.DoublePrecision
    M35 Float? @db.DoublePrecision
    M40 Float? @db.DoublePrecision
    M45 Float? @db.DoublePrecision
    M50 Float? @db.DoublePrecision
    M55 Float? @db.DoublePrecision
    M60 Float? @db.DoublePrecision
    M65 Float? @db.DoublePrecision
    M70 Float? @db.DoublePrecision

    // Female fields (00-70)
    F00 Float? @db.DoublePrecision
    F10 Float? @db.DoublePrecision
    F15 Float? @db.DoublePrecision
    F20 Float? @db.DoublePrecision
    F25 Float? @db.DoublePrecision
    F30 Float? @db.DoublePrecision
    F35 Float? @db.DoublePrecision
    F40 Float? @db.DoublePrecision
    F45 Float? @db.DoublePrecision
    F50 Float? @db.DoublePrecision
    F55 Float? @db.DoublePrecision
    F60 Float? @db.DoublePrecision
    F65 Float? @db.DoublePrecision
    F70 Float? @db.DoublePrecision

    updated_at DateTime? @default(now()) @db.Timestamp(6)

    @@id([YMD, TT, CELL_ID])
    @@map("seoul_250_population")
}
